;;; c-keybindings.el --- C/C++ mode keybindings -*- lexical-binding: t; -*-

;;; Commentary:

;; Local keybindings for c-mode and c++-mode.
;; Loaded from config.el when the C/C++ language step is activated.

;;; Code:

(provide 'c-keybindings)
;;; c-keybindings.el ends here
