;;; guile-keybindings.el --- Guile Scheme mode keybindings -*- lexical-binding: t; -*-

;;; Commentary:

;; Local keybindings for scheme-mode / Geiser Guile buffers.
;; Loaded from config.el when the Guile language step is activated.

;;; Code:

(provide 'guile-keybindings)
;;; guile-keybindings.el ends here
