;;; zig-keybindings.el --- Zig mode keybindings -*- lexical-binding: t; -*-

;;; Commentary:

;; Local keybindings for zig-mode.
;; Loaded from config.el when the Zig language step is activated.

;;; Code:

(provide 'zig-keybindings)
;;; zig-keybindings.el ends here
