;;; lua-keybindings.el --- Lua mode keybindings -*- lexical-binding: t; -*-

;;; Commentary:

;; Local keybindings for lua-mode.
;; Loaded from config.el when the Lua language step is activated.

;;; Code:

(provide 'lua-keybindings)
;;; lua-keybindings.el ends here
